// Core/constants/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
static const darkBg   = Color.fromARGB(255, 0, 0, 0);  // الخلفية (أسود مائل للبنفسجي)
static const grayWave = Color(0xFF62606A);  // المنحني الرمادي
static const white    = Colors.white;
static const blue = Color(0x728ED4);
}